# ==================================================
# Docker環境用セキュリティ設定
# ==================================================

# 基本設定
NODE_ENV=production

# セキュリティ設定
BEARER_TOKEN=your-secret-token
ALLOWED_ORIGINS=http://localhost:3000,https://localhost:3443

# HTTPS設定
ENABLE_HTTPS_REDIRECT=false

# Rate Limiting設定
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=5

# ログ設定
LOG_LEVEL=info

# ==================================================
# 本番環境では以下を変更してください:
# 1. BEARER_TOKENを強力なランダム文字列に変更
# 2. ALLOWED_ORIGINSを実際のドメインに設定
# 3. ENABLE_HTTPS_REDIRECTをtrueに設定（HTTPSを使用する場合）
# ==================================================